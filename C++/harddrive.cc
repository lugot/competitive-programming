#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int n, c, b;
	cin >> n >> c >> b;

	vector<int> broken(b);
	for(int &x: broken) cin >> x;

	vector<int> ans(n, 0);

	int setted = 0;
	for(int i=0; i<b; i++){
	}


	

	return 0;
}
