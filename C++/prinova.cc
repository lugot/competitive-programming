#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int N;
	cin >> N;

	vector<int> nums(N);
	for(auto &x: nums) cin >> x;

	int A,B;
	cin >> A >> B;

	vector<pair<int, int>> sols;

	for(int i=1; i<N; i++){
	}


	return 0;
}
