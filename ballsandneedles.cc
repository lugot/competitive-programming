#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int K;
	cin >> K;
	int x1,x2,y1,y2,z1,z2;
	
	vector<int> repr2(1e6), repr3(1e6);
	vector<vector<int>> d2[100000];
	vector<vector<int>> d3[100000];

	while(K--){
		cin >> x1 >> y1 >> z1 >> x2 >> y2 >> z2;
		



	}	

	return 0;
}
